language: python
sudo: false
python:
  - "2.6"
  - "2.7"

install:
  - pip install --upgrade .
  - pip install --upgrade -r requirements.txt
  - pip install coveralls

script: py.test -s -v tests --cov cdata

after_success:
  coveralls
